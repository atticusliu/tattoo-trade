import { redirect } from "next/navigation";
import AdminDashboard from "../(dashboards)/admin-dashboard/page";
import UserDashboard from "../(dashboards)/user-dashboard/page";

export default async function Dashboard() {
  /*
  if (userRole === 'admin') {
    return <AdminDashboard />;
  } else if (userRole === 'user') {
    return <UserDashboard />;
  } else {
    redirect('/login');
  }
  */
}